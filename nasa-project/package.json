{
  "name": "course",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-client": "npm install --prefix client",
    "install-server": "npm install --prefix server",
    "install": "npm run install-server && npm run install-client",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "watch": "npm run client & npm run server",
    "deploy": "npm run build --prefix client && npm run build --prefix server && npm run start --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deivicarlos/node-course.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/deivicarlos/node-course/issues"
  },
  "homepage": "https://github.com/deivicarlos/node-course#readme"
}